name: Integrity Checks

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

jobs:
  integrity_checks:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    name: API And SQL Integrity
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            api/package-lock.json

      - name: Install app dependencies
        run: npm ci

      - name: Install API dependencies
        run: npm ci --prefix api

      - name: Lint API
        id: lint_api
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run lint:api 2>&1 | tee api-lint.log

      - name: Validate setup SQL
        id: lint_sql
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run lint:sql 2>&1 | tee sql-lint.log

      - name: Validate API response contracts
        id: lint_api_responses
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run lint:api-responses:strict-ux 2>&1 | tee api-responses-lint.log

      - name: Validate upsert conflicts
        id: lint_upsert
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run lint:upsert-conflicts 2>&1 | tee upsert-lint.log

      - name: Validate error tracking (warn-only)
        id: lint_error_tracking
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run lint:error-tracking 2>&1 | tee error-tracking-lint.log

      - name: Type-check frontend (warn-only)
        id: lint_types
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          npm run lint:types 2>&1 | tee types-lint.log

      - name: Build integrity report
        id: report
        shell: bash
        run: |
          # This step only builds the report and writes overall_status; the actual
          # check results are already captured via each step's outcome. A non-zero
          # grep/tail/SIGPIPE here must NOT abort it (GitHub's default shell is -e
          # -o pipefail), so errexit/pipefail are explicitly disabled.
          set +e +o pipefail
          api_status="${{ steps.lint_api.outcome }}"
          sql_status="${{ steps.lint_sql.outcome }}"
          api_responses_status="${{ steps.lint_api_responses.outcome }}"
          upsert_status="${{ steps.lint_upsert.outcome }}"
          report_sha="${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.sha }}"
          report_branch="${{ github.event_name == 'pull_request' && github.event.pull_request.head.ref || github.ref_name }}"
          short_sha="$(printf '%s' "$report_sha" | cut -c1-8)"
          run_url="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          updated_at="$(date -u +'%Y-%m-%d %H:%M:%S UTC')"
          # Frontend type-check is warn-only: surface the issue count, never fail the job on it.
          types_count="$(grep -oE '\[typecheck\] [0-9]+ type' types-lint.log 2>/dev/null | grep -oE '[0-9]+' | head -n1 || true)"
          types_count="${types_count:-0}"
          if [ "$types_count" = "0" ]; then types_label="0 — clean ✅"; else types_label="${types_count} — warn-only ⚠️"; fi
          # Overall pass/fail = the blocking checks only (api, sql, api-responses, upsert).
          # Warn-only checks (error-tracking, frontend type-check) never affect it.
          if [ "$api_status" = "success" ] && [ "$sql_status" = "success" ] \
            && [ "$api_responses_status" = "success" ] && [ "$upsert_status" = "success" ]; then
            overall_status="success"
            status_icon="✅"
            status_line="All integrity checks passed."
          else
            overall_status="failure"
            status_icon="❌"
            status_line="One or more integrity checks failed."
          fi

          {
            echo "<!-- pr-health-integrity:start -->"
            echo "<!-- integrity-meta api=${api_status} sql=${sql_status} apiResponses=${api_responses_status} upsert=${upsert_status} types=${types_count} overall=${overall_status} sha=${short_sha} -->"
            echo "## ${status_icon} Integrity Checks"
            echo
            echo "> __CHANGE_SUMMARY__"
            echo
            echo "${status_line}"
            echo
            echo "- Branch: \`${report_branch}\`"
            echo "- Commit: \`${short_sha}\`"
            echo "- Updated: ${updated_at}"
            echo "- Workflow run: [Open run](${run_url})"
            echo
            echo "| Check | Result |"
            echo "| --- | --- |"
            echo "| API lint | ${api_status} |"
            echo "| API response contracts | ${api_responses_status} |"
            echo "| Upsert conflicts | ${upsert_status} |"
            echo "| SQL lint | ${sql_status} |"
            echo "| Error tracking (warn-only) | see output |"
            echo "| Frontend type-check (warn-only) | ${types_label} |"
            echo
            echo "<details><summary>API lint output</summary>"
            echo
            echo '```text'
            tail -n 200 api-lint.log
            echo '```'
            echo
            echo "</details>"
            echo
            echo "<details><summary>SQL lint output</summary>"
            echo
            echo '```text'
            tail -n 200 sql-lint.log
            echo '```'
            echo
            echo "</details>"
            echo
            echo "<details><summary>API response contracts output</summary>"
            echo
            echo '```text'
            tail -n 200 api-responses-lint.log
            echo '```'
            echo
            echo "</details>"
            echo
            echo "<details><summary>Upsert conflicts output</summary>"
            echo
            echo '```text'
            tail -n 200 upsert-lint.log
            echo '```'
            echo
            echo "</details>"
            echo
            echo "<details><summary>Error tracking output (warn-only)</summary>"
            echo
            echo '```text'
            tail -n 200 error-tracking-lint.log
            echo '```'
            echo
            echo "</details>"
            echo
            echo "<details><summary>Frontend type-check output (warn-only)</summary>"
            echo
            echo '```text'
            tail -n 200 types-lint.log
            echo '```'
            echo
            echo "</details>"
            echo
            echo "<!-- pr-health-integrity:end -->"
          } > integrity-report.md

          echo "overall_status=${overall_status}" >> "$GITHUB_OUTPUT"

      - name: Upload integrity logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integrity-check-logs
          path: |
            api-lint.log
            sql-lint.log
            api-responses-lint.log
            upsert-lint.log
            error-tracking-lint.log
            types-lint.log
            integrity-report.md

      - name: Comment report on pull request
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        env:
          REPORT_PATH: integrity-report.md
        with:
          script: |
            const fs = require('fs');
            let sectionBody = fs.readFileSync(process.env.REPORT_PATH, 'utf8');
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const rootMarker = '<!-- pr-health-report -->';
            const legacyMarker = '<!-- integrity-check-report -->';
            const sectionStart = '<!-- pr-health-integrity:start -->';
            const sectionEnd = '<!-- pr-health-integrity:end -->';
            const metaPattern = /<!-- integrity-meta api=(\w+) sql=(\w+) apiResponses=(\w+) upsert=(\w+) types=(\d+) overall=(\w+) sha=([0-9a-f]+) -->/;

            const upsertSection = (body, startMarker, endMarker, nextSection) => {
              const startIndex = body.indexOf(startMarker);
              const endIndex = body.indexOf(endMarker);
              if (startIndex >= 0 && endIndex >= 0 && endIndex >= startIndex) {
                return `${body.slice(0, startIndex)}${nextSection}${body.slice(endIndex + endMarker.length)}`.trim();
              }
              return `${body.trim()}\n\n${nextSection}`.trim();
            };

            const comments = await github.paginate(github.rest.issues.listComments, {
              owner,
              repo,
              issue_number,
              per_page: 100,
            });
            const existing = comments.find((comment) =>
              comment.user?.type === 'Bot'
                && typeof comment.body === 'string'
                && (comment.body.includes(rootMarker) || comment.body.includes(legacyMarker)),
            );
            const currentMetaMatch = sectionBody.match(metaPattern);
            const currentMeta = currentMetaMatch ? {
              api: currentMetaMatch[1],
              sql: currentMetaMatch[2],
              apiResponses: currentMetaMatch[3],
              upsert: currentMetaMatch[4],
              types: currentMetaMatch[5],
              overall: currentMetaMatch[6],
              sha: currentMetaMatch[7],
            } : null;
            const previousMetaMatch = existing?.body?.match(metaPattern);
            const previousMeta = previousMetaMatch ? {
              api: previousMetaMatch[1],
              sql: previousMetaMatch[2],
              apiResponses: previousMetaMatch[3],
              upsert: previousMetaMatch[4],
              types: previousMetaMatch[5],
              overall: previousMetaMatch[6],
              sha: previousMetaMatch[7],
            } : null;

            const humanizeCheck = (value) => {
              if (value === 'success') return 'passed';
              if (value === 'failure') return 'failed';
              return value || 'unknown';
            };

            const changeLines = [];
            if (!previousMeta || !currentMeta) {
              changeLines.push('First integrity report for this pull request.');
            } else {
              if (previousMeta.api !== currentMeta.api) {
                changeLines.push(`API lint changed from ${humanizeCheck(previousMeta.api)} to ${humanizeCheck(currentMeta.api)}.`);
              }
              if (previousMeta.sql !== currentMeta.sql) {
                changeLines.push(`SQL lint changed from ${humanizeCheck(previousMeta.sql)} to ${humanizeCheck(currentMeta.sql)}.`);
              }
              if (previousMeta.apiResponses !== currentMeta.apiResponses) {
                changeLines.push(`API response contracts changed from ${humanizeCheck(previousMeta.apiResponses)} to ${humanizeCheck(currentMeta.apiResponses)}.`);
              }
              if (previousMeta.upsert !== currentMeta.upsert) {
                changeLines.push(`Upsert conflicts changed from ${humanizeCheck(previousMeta.upsert)} to ${humanizeCheck(currentMeta.upsert)}.`);
              }
              if (previousMeta.types !== currentMeta.types) {
                changeLines.push(`Frontend type-check issues changed from ${previousMeta.types} to ${currentMeta.types} (warn-only).`);
              }
              if (!changeLines.length) {
                changeLines.push(`No status change since commit \`${previousMeta.sha}\`.`);
              }
            }

            sectionBody = sectionBody.replace('__CHANGE_SUMMARY__', changeLines.join(' '));
            const baseBody = existing?.body?.includes(rootMarker)
              ? existing.body
              : [
                rootMarker,
                '## PR Health Report',
                '',
                '_This comment is updated automatically with the latest integrity and deployment status for this pull request._',
              ].join('\n');
            const nextBody = upsertSection(baseBody, sectionStart, sectionEnd, sectionBody);
            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body: nextBody,
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number,
                body: nextBody,
              });
            }

      - name: Fail job if integrity checks failed
        if: steps.report.outputs.overall_status != 'success'
        shell: bash
        run: |
          echo "Integrity checks failed."
          exit 1
